{
  "buildCommand": "pnpm gen:runtime && cp next.config.edgeone.js next.config.js && pnpm next build && rm -rf .next/cache && find .next -name '*.pack' -delete",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": ".next",
  "nodeVersion": "22.11.0"
}
